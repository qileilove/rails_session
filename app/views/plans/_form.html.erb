<%= form_for([@idea, @idea.plans.build]) do |f| %>
    <p>
      <%= f.label :plan_name %><br>
      <%= f.text_field :plan_name %>
    </p>
    <p>
      <%= f.label :desc %><br>
      <%= f.text_area :desc %>
    </p>
    <p>
      <%= f.submit %>
    </p>
<% end %>